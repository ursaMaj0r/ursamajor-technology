{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Jekyll: Build Dev",
            "detail": ". ./.devcontainer/.env && bundle install && npm install && npm run dev && npm start",
            "type": "shell",
            "linux": {
                "command": ". ./.devcontainer/.env && bundle install && npm install && npm run dev && npm start"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "presentation": {
                "echo": true,
                "reveal": "always",
                "focus": false,
                "panel": "shared",
                "showReuseMessage": true,
                "clear": false
            },
            "problemMatcher": [
                "$eslint-stylish",
                "$jekyll-error-watch",
                "$jekyll-warning-watch"
            ]
        }
    ]
}